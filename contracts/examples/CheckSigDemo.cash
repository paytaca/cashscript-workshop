pragma cashscript ^0.13.0;
contract CheckSigDemo(pubkey publicKeyOfContractOwner) {
    function spend(sig signatureOfContractOwner) {
        require(checkSig(signatureOfContractOwner, publicKeyOfContractOwner));
    }
}